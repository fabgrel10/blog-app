<div class="flex mb-3">
  <div class="bg-white border border-sky-500">
    <%= user_avatar(user) %>
  </div>
  <div class="bg-white border border-sky-500 ml-5 p-3 w-full">
    <h2 class="text-2xl font-bold"><%= user.name %></h2>
    <p class="text-gray-700 text-right">
      Number of posts: <%= user.posts.count %>
    </p>
  </div>
</div>
