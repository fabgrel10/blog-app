<div class="p-5 bg-white border shadow-lg border-sky-500">
  <h2 class="mb-2 text-lg font-bold text-gray-800">
    <%= post.title %> by <%= post.author.name %>
    <span class="flex float-right text-xs text-gray-700">
      Comments: <%= post.comments_counter %> Likes: <%= post.likes_counter %>
    </span>
  </h2>
  <p class="my-3 text-justify text-gray-700"><%= post.text %></p>
  <%= button_to 'Like',
      user_post_likes_path(post_id: post.id),
      class: 'mb-3 p-2 font-bold text-center text-gray-900 bg-white border rounded focus:outline-none border-sky-500 hover:bg-sky-500 hover:text-white' %>
</div>
